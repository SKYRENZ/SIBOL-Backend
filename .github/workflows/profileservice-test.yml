name: Profile Service Tests

on:
  push:
    paths:
      - 'src/services/profileService.ts'
      - 'src/test/profileService.test.ts'
      - '.github/workflows/profileservice-test.yml'
  pull_request:
    paths:
      - 'src/services/profileService.ts'
      - 'src/test/profileService.test.ts'

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: sibol_draft
        ports:
          - 3306:3306

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Wait for MySQL to be ready
        run: |
          for i in {1..60}; do
            mysqladmin -h 127.0.0.1 -u root -proot ping && break
            sleep 1
          done

      - name: Create database tables for ProfileService
        run: |
          mysql -h 127.0.0.1 -u root -proot sibol_draft -e "
            CREATE TABLE IF NOT EXISTS accounts_tbl (
              Account_id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
              Username VARCHAR(100) NOT NULL,
              Password VARCHAR(255) NOT NULL,
              Roles INT(11) DEFAULT NULL,
              IsActive TINYINT(1) DEFAULT 1,
              Account_created TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP()
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

            CREATE TABLE IF NOT EXISTS profile_tbl (
              Profile_id INT(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
              Account_id INT(11) DEFAULT NULL,
              FirstName VARCHAR(100) NOT NULL,
              LastName VARCHAR(100) NOT NULL,
              Area_id INT(11) DEFAULT NULL,
              Contact BIGINT(20) DEFAULT NULL,
              Email VARCHAR(150) UNIQUE,
              QR_code VARCHAR(255) DEFAULT NULL,
              Profile_last_updated TIMESTAMP NULL DEFAULT NULL,
              FOREIGN KEY (Account_id) REFERENCES accounts_tbl(Account_id)
            ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
          "

      - name: Run ProfileService tests
        env:
          DB_HOST: localhost
          DB_USER: root
          DB_PASS: root
          DB_NAME: sibol_draft
        run: npx jest src/test/profileService.test.ts --runInBand